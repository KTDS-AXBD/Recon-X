---
id: AIF-PLAN-045
title: "F417 — LPON skill source data 고도화 PoC (43건 LLM augmentation)"
sprint: 248
f_items: [F417]
req: AIF-REQ-042
status: PLANNED
created: "2026-05-03"
author: "Sprint Autopilot"
---

# F417 Plan — LPON skill source data 고도화 PoC

## 1. 목표

Sprint 247 F416 LPON 35건 single eval 결과에서 **0% PASS** 3 criteria(io_structure / exception_handling / testability)를 LLM augmentation으로 해소한다.

**근본 원인 (확정)**:
- `spec-content-adapter.ts:buildRulesTable()` — exception 컬럼에 `p.source.excerpt ?? "—"` 사용. LPON 번들은 `source.excerpt`가 원문 발췌 텍스트이거나 없어서 사실상 모두 "—" 렌더 → exception_handling rubric 0점
- `spec-content-adapter.ts:tests` 생성 — `"${p.condition} 조건 충족"` 추상 템플릿 사용 → io_structure / testability rubric 0점 (구체값 없음, 에러 케이스 없음)

**범위**: LPON 35 (번들 스킬) + lpon 7 (spec-container 스킬) = 42건

## 2. 4-Step 실행 계획

| Step | 소요 | 내용 |
|------|------|------|
| Step 1 | 0.5h | 사전 분석 — 43건 R2 번들 다운로드 + exception 공백률 정량화 |
| Step 2 | 2h | `augment-skill-data.ts` 신설 — LLM(Haiku) augmentation |
| Step 3 | 1.5h | `spec-content-adapter.ts` 수정 + svc-skill 재배포 + 43건 재평가 |
| Step 4 | 1h | 검증 + 분석 + SPEC 동기화 |

## 3. 범위 파일 목록

| 파일 | 변경 유형 | 설명 |
|---|---|---|
| `scripts/ai-ready/augment-skill-data.ts` | 신규 | LLM augmentation + R2 write script |
| `services/svc-skill/src/ai-ready/spec-content-adapter.ts` | 수정 | description 필드에서 test scenarios 파싱 |
| `services/svc-skill/src/routes/ai-ready.ts` | 수정 | `r2KeyOverride` 요청 바디 필드 지원 |
| `scripts/ai-ready/augment-eval-loop.ts` | 신규 | augmented path로 재평가 loop |

## 4. DoD 매트릭스

| 기준 | 현재 (Before) | 목표 (After) |
|------|-------------|-------------|
| exception_handling PASS율 | 0/35 (0%) | ≥ 22/43 (50%) |
| io_structure PASS율 | 0/35 (0%) | ≥ 22/43 (50%) |
| testability PASS율 | 0/35 (0%) | ≥ 22/43 (50%) |
| avg_score | 0.511 | ≥ 0.65 |
| R2 augmented 번들 | — | 43건 `skill-packages/augmented/{id}.skill.json` |
| R2 write 검증 | — | 5/5 sample HTTP 200 |
| 재평가 보고서 | — | `reports/ai-ready-LPON-augmented-{date}.json` ≥ 30KB |
| LLM augmentation 비용 | — | ≤ $1.50 |
| 재평가 비용 | — | ≤ $0.30 |
| Match Rate | — | ≥ 90% |
| AIF-REQ-042 상태 | PLANNED | DONE |

## 5. 리스크 및 실패 기준

| 리스크 | 대응 |
|-------|------|
| 3 criteria 통과율 50% 미달 | augmentation prompt iterate 1회 + 미달 사유 분석 후 Sprint 종결 (248 결과 기록) |
| LLM augmentation 비용 $1.50 초과 | 8건 sample 재실행 + 통과율 추정 보고 |
| policy ID dedup 의미 손실 우려 | dedup 보류 — 중복 ID 그대로 보존 |
| svc-skill 배포 실패 | `r2KeyOverride` 없이 standard r2_key 사용하는 fallback evaluation |

## 6. 제외 범위 (Out-of-scope)

- `PolicyCandidateSchema`에 `exception` 필드 정식 확장 (별도 Sprint)
- svc-policy 추론 프롬프트 갱신
- 5,214 전수 적용 (TD-57 batch 정상화 후)
- Miraeasset 도메인
- srp_reusability / source_consistency 개선
