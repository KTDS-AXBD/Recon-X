---
id: AIF-ANLS-034
title: F412 Sprint 244 PARTIAL FAIL 분석 — Queue burst LLM HTML index 응답 + LPON R2 누락
type: analysis
sprint: Sprint 244
session: 255
date: 2026-05-03
status: complete
related:
  - AIF-PLAN-044 (F412 Plan)
  - AIF-RPRT-044 (F412 Report)
  - AIF-REQ-041
  - TD-55 (LPON R2 누락)
  - TD-56 (Queue burst LLM HTML)
---

# F412 Analysis — Sprint 244 PARTIAL FAIL

## 1. 요약

Sprint 244 F412 LPON 35건 batch 운영 실행은 **PARTIAL FAIL**. 4 Step 단계별 진행은 모두 완료되었으나, batch 실 결과 산출(D1 정상 score row + reports/ KPI 통과율)은 두 차단으로 미달성:

1. **TD-55 LPON 35 R2 누락** — bundled skills의 R2 `skill-packages/{id}.skill.json` 100% 미존재
2. **TD-56 Queue burst LLM HTML index 응답** — lpon 8 batch 8/8 process 완료하였으나 LLM call 48/48 fail (HTML index 응답 → JSON parse error)

evaluator 자체 + Single evaluate 경로는 정상 동작 (Master smoke 2회 모두 6/6 criteria 정상 score 반환). 차단은 systemic infra 이슈로 별도 sprint 해소 필요.

## 2. 단계별 결과

### Step 1 — 사전 환경 검증 ✅

- `~/.secrets/decode-x-internal` 64bytes hex (chmod 600) ✅
- `~/.secrets/openrouter-api-key` 73bytes (chmod 600) ✅
- `https://svc-skill.ktds-axbd.workers.dev/health` HTTP 200 (0.4s) ✅
- batch dry-run `--organization LPON` → totalSkills=35, estimatedCostUsd=$0.81, estimated 3min ✅

### Step 2 — Master production smoke (lpon-charge 4591b69e) ✅

```bash
POST /skills/4591b69e-4e6a-4ac8-8261-ce177c35f994/ai-ready/evaluate?force=true
```

**1차 결과** (06:05:53):
- HTTP 200, 7.2s, costUsd $0.0036
- totalScore 0.54, passCount 1, overallPassed false
- 6/6 criteria 정상 score 반환:
  - source_consistency 0.25, comment_doc_alignment 0.95, io_structure 0.42
  - exception_handling 0.62, srp_reusability 0.58, testability 0.42

**2차 결과** (06:16:22, 진단 retry):
- HTTP 200, 8.7s, costUsd $0.0036
- totalScore 0.443, passCount 1, overallPassed false
- 5/6 criteria 정상 score, 1/6 (srp_reusability) score=0 — temperature 0.1 noise

**판정**: evaluator + LLM endpoint(CLOUDFLARE_AI_GATEWAY_URL) + skill-package R2 (lpon 8 reviewed) 모두 정상 동작 확정.

### Step 3 — Mixed scope batch 실행 ❌

#### Step 3a — LPON 35 R2 누락 증빙 (5/5 sample)

```
POST /skills/{id}/ai-ready/evaluate force=true 5건 시도
```

| Sample skill_id | HTTP | error.code | error.message |
|---|---|---|---|
| 5281357c-bf73-41eb-a5b9-57bb8899f1e0 | 404 | NOT_FOUND | `spec-container '...' not found` |
| c5a73dc0-8c13-424f-9593-adfd2b758357 | 404 | NOT_FOUND | 동일 |
| a7548a83-2a84-4bff-aaff-e9f6c05d26fe | 404 | NOT_FOUND | 동일 |
| d42c15f9-437c-4f6f-a5d7-b2892992f69f | 404 | NOT_FOUND | 동일 |
| 91d347e7-5b01-4a01-863d-129403f152a6 | 404 | NOT_FOUND | 동일 |

**근본 원인**: routes/ai-ready.ts:142 `loadSpecContent(env, skillId, organizationId)` — Primary path는 `skill-packages/{skillId}.skill.json`이나 LPON 35 bundled skills의 해당 R2 key가 존재하지 않음. CLAUDE.md 알려진 issue (`Bundled skills R2 이슈: rebundle로 생성된 bundle 파일은 R2에 미업로드 (개별 superseded skills만 R2 존재)`) 재확인.

**부수 발견**: `routes/ai-ready.ts:144` `notFound("spec-container", skillId)` — 메시지 라벨이 "spec-container"이나 실제 Primary path는 `skill-packages/`. Master 디버깅 시 misdirect 야기. cosmetic fix 후보.

#### Step 3b — lpon 8 reviewed batch 실행 ❌ (process 완료, LLM 100% fail)

```
POST /skills/ai-ready/batch
body: {model:"haiku", organizationId:"lpon", crossCheckSampleSize:0}
batchId: f4caf9e8-cfee-49af-861a-ae90d63cb9c5
```

**Batch status (D1 truth)**:
| 필드 | 값 |
|------|----|
| status | completed |
| totalSkills | 8 |
| completedSkills | 8 |
| failedSkills | 0 |
| totalCostUsd | $0.0288 |
| avgScore | 0 |
| startedAt | 2026-05-03T06:06:16.270Z |
| completedAt | 2026-05-03T06:06:31.537Z |
| **소요** | **15초** (예상 3분 대비 12배 빠름 → fast-fail signal) |

**Raw evaluations 검증** (4591b69e lpon-charge, 6 criteria):

전체 6 criteria 모두 score=0, rationale 동일:
```
Evaluation failed: SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON
```

8 skill × 6 criteria = 48 LLM call 100% fail.

## 3. Root Cause 진단 — TD-56

### 3.1 가설 정립

`packages/utils/src/llm-client.ts:118` `callLlmRouterWithMeta` 흐름:
1. `fetch(env.CLOUDFLARE_AI_GATEWAY_URL, POST, Authorization Bearer OPENROUTER_API_KEY)`
2. response.ok=true(HTTP 200) + body=HTML(`<!DOCTYPE`)이면 `response.json()`이 SyntaxError throw
3. `evaluator.ts:159` catch (e) → score=0, rationale=`Evaluation failed: ${String(e)}`

**Single evaluate** (Master smoke) ✅ 정상 → endpoint URL 자체 정상. **Batch Queue** ❌ 100% fail → Queue 환경에서만 systemic.

### 3.2 가설 후보

| # | 가설 | 가능성 | 검증 방법 |
|---|------|--------|----------|
| H1 | OpenRouter rate limit (free tier) — burst 시 일부 call HTML rate limit page | 중 | OpenRouter dashboard log + 단계적 throttle test |
| H2 | CF AI Gateway rate limit — 같은 burst 시 HTML index | 중 | CF AI Gateway dashboard 상태 + auth=False 모드 다시 점검 |
| H3 | CF Worker subrequest 한도 50/request 초과 — Queue handler에서 6 LLM call × 동시 batch_size 가능성 | 중상 | wrangler.toml ai-ready-queue `max_batch_size` 확인 |
| H4 | Anthropic API rate limit (OpenRouter 통한) | 중 | Anthropic dashboard 또는 OpenRouter passthrough 응답 헤더 |
| H5 | Queue context env vars drift — production env에서 staging URL 호출 | 낮 | Queue handler에서 env.CLOUDFLARE_AI_GATEWAY_URL 직접 logging |
| H6 | timing: Master smoke 직후 23s에 batch trigger → 캐시 invalidation race | 낮 | 5분 idle 후 batch 재시도 |

### 3.3 가설 시그널 강화

**H3 — CF Worker subrequest 한도 50/request 가설** 강화 시그널:
- batch 15초 fast-fail = 모든 LLM call이 시작과 거의 동시에 fail
- 8 skill × 6 LLM call = 48 subrequest 동시 시도 → 50 한도에 근접
- Queue concurrency 10 + 동시 처리 시 worker isolate 1개당 최대 60 subrequest 가능

**H1/H2 가설** — OpenRouter/CF AI Gateway free-tier rate limit:
- Master smoke 직후 burst 발생 시 token bucket 비어있음 → 즉시 limit
- 그러나 Master smoke 2차 retry (06:16:22, batch 후 ~10분)는 정상 → bucket 회복됨

**가장 likely**: H1+H3 조합 — OpenRouter burst rate limit + CF Worker subrequest budget 동시 압박. fix 방향:
- Queue concurrency 1~3으로 throttle (max_concurrency 10 → 3)
- evaluator에서 6 criteria를 sequential 호출로 변경 (현재 Promise.all 병렬)
- callLlmRouterWithMeta에 retry+exponential backoff (HTTP 200 + Content-Type !json 검출 시 재시도)

### 3.4 추가 진단 비용

정확한 root cause 확정에는 1~2h 추가 필요:
- `wrangler tail svc-skill --env production` 30초 + 동시 batch 재시도 → runtime error log 확보
- Queue concurrency 1 일시 변경 후 재배포 + batch 재시도 → burst 가설 검증
- packages/utils/src/llm-client.ts에 response.headers.get("content-type") logging 추가

본 Sprint 시간 효율 측면에서 진단 deferred. **TD-56 신규 등록**으로 별도 sprint 해소 권고.

## 4. DoD 충족 매트릭스

| DoD | 원안 | 결과 | 비고 |
|-----|------|------|------|
| D1 LPON 210 row | 35 × 6 = 210 정상 score | ❌ 0 (R2 누락) + lpon 48 (모두 score=0) | TD-55 / TD-56 |
| reports/ ≥ 30KB | ai-ready-LPON-{date}.json | ❌ 784 bytes (KPI only) | 다만 5건 합계 14KB+ |
| Haiku 6기준 평균 통과율 | 정상 분포 | ❌ 0.0% | TD-56 |
| Master smoke HTTP 200 | 1건 | ✅ 2회 (lpon-charge) | |
| analysis 문서 | 1 파일 | ✅ AIF-ANLS-034 | |
| Match Rate ≥ 90% | 95% 목표 | 🟡 단계 진행 100% / DoD 산출 50% | |
| Plan/Report 문서 | 2 파일 | ✅ AIF-PLAN-044 + AIF-RPRT-044 | |

## 5. Sprint 244 판정

**PARTIAL FAIL** — 단계별 진행 + 진단은 완료, 산출물은 차단.

- ✅ Process 완료: Step 1~4 모두 진행, Master 직접 진단 (autopilot 9회차 패턴 회피)
- ✅ Root cause 시그널 확정: endpoint OK + Queue burst만 fail
- ❌ 원 scope (LPON 35건 batch) 0% 평가
- ✅ 차단 2건 명확히 분리 + 신규 TD 등록 가능
- ✅ 정상 동작 증빙 (single evaluate 2회) 확보 → 시스템 일부 정상 확인

## 6. 후속

### TD-55 — LPON 35 R2 누락 (P1)
- packaging pipeline에서 bundle 파일 R2 업로드 누락 수정 또는 855 superseded skills cleanup
- 추정: 1~2 sprint

### TD-56 — Queue burst LLM HTML index 응답 (P1)
- Queue concurrency throttle (10 → 3) 또는 evaluator sequential criteria 호출
- callLlmRouterWithMeta retry + content-type 검증 로직
- 추정: 0.5~1 sprint

### F412 재시도 — Sprint 245+
- TD-55/56 해소 후 LPON 35건 batch 재실행
- 또는 lpon 8건 single evaluate loop으로 우회 시 ~72초 + 정상 reports/

## 7. 신규 교훈

1. **batch endpoint 자체 정상 ≠ Queue 경로 정상** — Single evaluate와 Queue burst는 같은 evaluator를 호출하나 다른 fail 모드. 사전 검증은 단건 + burst 양쪽 필요
2. **batch 15초 fast-fail = LLM systemic 문제** — 정상 batch는 8 skill × ~9초 = ~72초 예상. 빠른 완료는 fast-fail signal
3. **avg_score=0 + completedSkills=8 + failedSkills=0**은 silent failure 위험 — Queue가 score=0을 정상 insert로 처리. 별도 quality gate 필요
4. **CLAUDE.md 알려진 issue를 Sprint 진입 전 사전 점검 필수** — "Bundled skills R2 이슈" 메모를 Sprint 244 plan 단계에서 사전 검증했더라면 scope 재조정 가능
5. **routes/ai-ready.ts:144 `notFound("spec-container", id)` 메시지 라벨이 Primary path와 불일치** — 디버깅 시 misdirect 야기 (Master가 처음 Legacy path 호출 의심함). cosmetic fix
