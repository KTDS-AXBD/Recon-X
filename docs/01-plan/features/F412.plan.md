---
id: AIF-PLAN-044
title: F412 AI-Ready LPON 35건 batch 운영 실행 (F356-B 후속 축소판)
type: plan
sprint: Sprint 244
session: 255
date: 2026-05-03
status: PARTIAL_FAIL
related:
  - AIF-REQ-041
  - AIF-REQ-035 (F356-B 원안)
  - AIF-REQ-040 (F413 TD-53 해소)
  - AIF-PLAN-040 (F356-B 재활용)
---

# F412 Plan — AI-Ready LPON 35건 batch 운영 실행

## 1. 배경

Sprint 241 F413 ✅ MERGED + Production verified (LPON dry-run HTTP 200, totalSkills=35) → TD-53 ✅ 해소로 batch endpoint filter `status IN ('bundled','reviewed')` LPON 35건 매칭 정상화. F356-B 원안 5,154 전수($48 30~40분) 대신 LPON 35건 한정 batch(~$2)로 축소 — Master smoke 검증 + reports/ 실파일 산출이 일차 목표.

## 2. 4 Step (~3h estimate)

| Step | 작업 | 시간 |
|------|------|------|
| 1 | 사전 환경 검증 (production secrets 점검) | 0.3h |
| 2 | Master production smoke (`POST /skills/{id}/ai-ready/evaluate?force=true`) | 0.5h |
| 3 | batch 실행 (`scripts/ai-ready/batch-evaluate.ts --env production --model haiku --organization LPON --cross-check 0`) | 1.5h |
| 4 | 결과 검증 + analysis 문서 | 0.7h |

## 3. DoD (원안)

- D1 `ai_ready_scores WHERE organization='LPON'` ≥ 210 row (35 × 6)
- `reports/ai-ready-LPON-{date}.json` 실파일 ≥ 30KB
- Haiku 6기준 평균 통과율 산출
- Master smoke `POST /skills/{id}/ai-ready/evaluate` HTTP 200
- analysis 문서 (`docs/03-analysis/features/F412.analysis.md`)
- Match Rate ≥ 90%

## 4. R1~R3

- **R1**: production OPENROUTER_API_KEY rotation 미수행(MEMORY 보안 후속 line 1번)으로 만료/rate limit 차단 가능
- **R2**: batch 실 소요 10분 초과 시 부분 결과로 reports/ 산출 후 Sprint 245 잔여
- **R3**: 6기준 중 source_consistency JSON parse error 1회 stability 이슈(세션 246) 재현 시 N/A 마킹 + TD 신규

## 5. 실 결과 (PARTIAL FAIL)

상세는 `docs/03-analysis/features/F412.analysis.md` (AIF-ANLS-034) 및 `docs/04-report/features/F412.report.md` (AIF-RPRT-044) 참조.

### 차단 2건

1. **TD-55 — LPON 35 R2 누락** (CLAUDE.md 알려진 issue 재확인)
   - LPON 5/5 sample evaluate HTTP 404 `spec-container '...' not found`
   - 원인: rebundle로 생성된 bundle 파일이 R2 `skill-packages/{id}.skill.json`에 미업로드
   - 영향: F412 원 scope (LPON 35건) 100% 평가 불가

2. **TD-56 — Queue burst LLM HTML index 응답** (신규 발견)
   - lpon 8 batch trigger HTTP 200 + Queue process 8/8 완료 status `completed`
   - 그러나 LLM call 48/48 모두 fail (`SyntaxError: Unexpected token '<', "<!DOCTYPE "`)
   - avg_score=0, 모든 criterion score=0
   - Single evaluate(force=true)는 정상 작동 (Master smoke 2회 모두 6/6 정상 score 반환)
   - 원인 가설: CF Worker subrequest 한도 50/request 초과 또는 CF AI Gateway/OpenRouter burst rate limit

### 정상 증빙

- lpon-charge single evaluate 1차: HTTP 200 7.2s totalScore 0.54 (6/6 criteria 정상)
- lpon-charge single evaluate 2차: HTTP 200 8.7s totalScore 0.443 (5/6 criteria 정상, 1개 noise)
- evaluator + adapter + R2 layout (lpon reviewed 8건) + LLM endpoint 자체 = 정상

### DoD 충족 현황

| DoD | 결과 |
|-----|------|
| D1 LPON 210 row | ❌ 0 (LPON 35 R2 누락) — 다만 lpon 48 row insert (모두 score=0) |
| reports/ ≥ 30KB | ❌ batch fail로 KPI summary만 (~800 bytes) — 다만 5건 reports/ 합계 14KB+ 산출 |
| Haiku 평균 통과율 | ❌ 0.0% (batch fail) — 단 single eval 50% (3/6 criteria pass on 4591b69e) |
| Master smoke 200 | ✅ 2회 정상 |
| analysis 문서 | ✅ 작성 |
| Match Rate 90% | 🟡 PARTIAL (단계별 진행 100%, DoD 산출물 측면 50%) |

## 6. 차기 sprint 제안

- **TD-55 해소 (P1)**: packaging pipeline에서 bundle 파일 R2 업로드 누락 수정. 859 superseded skills R2 cleanup 또는 bundle 파일 재 packaging
- **TD-56 해소 (P1)**: Queue concurrency 1로 시도 + wrangler tail 30초 + svc-llm-router/CF AI Gateway burst trace. fix 후 batch 재시도

## 7. 참조

- `docs/03-analysis/features/F412.analysis.md` (AIF-ANLS-034)
- `docs/04-report/features/F412.report.md` (AIF-RPRT-044)
- `reports/ai-ready-lpon-2026-05-03.{json,md}` (batch fail 증빙)
- `reports/ai-ready-LPON-NOT_FOUND-2026-05-03.json` (R2 누락 5/5 증빙)
- `reports/ai-ready-lpon-charge-single-eval-{1,2}-2026-05-03.json` (정상 동작 2회 증빙)
- AIF-PLAN-040 (F356-B 원 Plan, §6 Phase 4 재활용)
- F413 (TD-53 해소, AIF-PLAN-043, AIF-REQ-040)
