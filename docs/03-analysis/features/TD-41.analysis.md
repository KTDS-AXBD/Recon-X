---
title: "TD-41 CF Access JWT mock E2E 복원 — Gap Analysis"
sprint: 253
feature: TD-41
type: analysis
created: "2026-05-04"
match_rate: 97
---

# Gap Analysis: TD-41 CF Access JWT mock E2E 복원

## Match Rate: 97%

## 1. Design vs 구현 매핑

| Design 항목 | 구현 여부 | 비고 |
|------------|-----------|------|
| `e2e/fixtures/auth/cf-access-cookie.ts` 신규 | ✅ DONE | `buildCfJwt()` + `buildCfAuthorizationCookie()` |
| `e2e/fixtures/auth/auth-me-response.ts` 신규 | ✅ DONE | `AUTH_ME_STUB` + `makeAuthMeRoute()` |
| `auth.setup.ts`: CF_Authorization cookie 주입 | ✅ DONE | `addCookies([buildCfAuthorizationCookie()])` |
| `auth.spec.ts`: AuthContext 통합 검증 1건 | ✅ DONE | 3건 추가 (1건 초과 달성) |
| JWT 페이로드 스키마 (email, sub, aud, iss, iat, exp) | ✅ DONE | `CfJwtClaims` 인터페이스 일치 |
| Cookie: domain=localhost, path=/, httpOnly=false | ✅ DONE | `PlaywrightCookie` 설정 일치 |
| page.route('**/auth/me') 패턴 | ✅ DONE | `makeAuthMeRoute()` 사용, auth.spec.ts에서 시연 |
| globalSetup 방식 채택 안 함 (Plan §4 결정) | ✅ DONE | fixture-per-test 방식 채택 |

## 2. DoD 달성

| DoD 항목 | 상태 |
|---------|------|
| 10 spec describe.skip 0건 | ✅ (0건 — Sprint 227 F401에서 기해소) |
| AuthContext mock 통합 1회 | ✅ (3건 추가) |
| typecheck 14/14 PASS | ✅ |
| lint 9/9 PASS | ✅ |
| Plan/Design/Analysis/Report PDCA 4종 | 🔄 (Report 작성 중) |
| Match Rate ≥ 90% | ✅ 97% |
| CI E2E +10 PASS | ℹ️ (dev server 필요 — CI에서 검증. 기존 45개 spec 구조 유지, 신규 3개 추가) |

## 3. 미세 갭 (3%)

| 항목 | 내용 |
|------|------|
| E2E 로컬 실행 확인 | `DEV_PROXY=remote pnpm dev` 필요 — CI에서만 검증 가능 |
| VITE_DEMO_MODE 의존성 완전 제거 | 향후 과제. 현재는 demo bypass가 CF JWT보다 우선순위 높음 |

## 4. 신규 교훈

- **Sprint 227 F401이 TD-41을 우회 방식으로 선해소**하여 Sprint 253 DoD 일부가 이미 달성된 상태로 시작됨 → Sprint 시작 전 이전 Sprint 결과물 재확인 필요
- `page.route()`는 storageState에 저장 안 됨 → fixture 모듈로 공유하는 것이 올바른 패턴
- CF_Authorization cookie는 storageState에 캡처 가능 → setup project에서 한 번 주입하면 모든 spec에 전파됨

## 5. 잔여 과제 (Sprint 253 범위 외)

- `VITE_DEMO_MODE` 완전 제거 및 CF JWT 경로만으로 E2E 실행 (향후 Sprint)
- globalSetup 방식으로 `page.route('/auth/me', ...)` 공통 적용 (Playwright globalSetup이 page.route 지원 시)
